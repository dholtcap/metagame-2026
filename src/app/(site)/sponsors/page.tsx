import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sponsors — Metagame 2026",
  description: "Sponsors of Metagame 2026.",
};

// The mock's sponsors page is intentionally just the shared header and footer.
export default function SponsorsPage() {
  return null;
}
